Debut
    - afficher une table de verité d'une fonction logique qui contient
            - des boucles pour les variables(a, b,c)de type booleen convertir en nombre 1 et 0
            -des resultats pour les fonction f(a, b, c)
     
     -afficher une premier forme canonique d'une fonction f(a, b, c) avec une parametre table qui contient :
            - une boucle du variable table qui affiche une fonction  logique des variables (a,b,c) et result
            -une condition: if row[-1]== 1:
                 if val == 1
                 elif val == 0
            
    -afficher une deuxième forme canonique d'une fonction f(a, b, c) avec une 
      avec une parametre table qui contient :
            -une boucle du variable table qui affiche une fonction logique des variables (a,b,c) et result
            -une condition: if row[-1]==0:
                 if val == 0
                 elif val == 1
        
Fin